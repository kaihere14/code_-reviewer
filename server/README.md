# Server (development)

Run the server in development with automatic restarts using nodemon:

```bash
cd server
npm install
npm run dev
```

This uses `nodemon` + `ts-node` and is configured by `nodemon.json`.
